syntax = "proto3";

package wal;

option go_package = "proto/walpb";

message WALRecord {
  string key = 1;
  uint64 offset = 2;
  optional uint64 timestamp = 3; // optional
  bool deleted = 4; // tombstone
  optional uint64 ttl = 5;
}